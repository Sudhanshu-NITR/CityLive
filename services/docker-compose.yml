services:
  report-service:
    build: ./report-service
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
    env_file:
      - ./report-service/.env

  api-gateway:
    build: ./api-gateway
    ports:
      - "8080:8080"
    depends_on:
      - report-service
    env_file:
      - ./api-gateway/.env

  event-service:
    build:
      context: ./event-service
    ports:
      - "8081:8081"
    restart: always
    env_file:
      - ./event-service/.env

  user-service:
    build:
      context: ./user-service
    ports:
      - "8082:8082"
    restart: always
