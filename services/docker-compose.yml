services:
  report-service:
    build: ./report-service
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development

  api-gateway:
    build: ./api-gateway
    ports:
      - "8080:8080"
    depends_on:
      - report-service
